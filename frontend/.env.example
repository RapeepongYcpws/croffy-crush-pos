# croffy-crush frontend configuration.
# Copy to .env.local for local dev (this file is committed; .env.local is gitignored).
#
# Base URL of the backend API, including the /api suffix.
#   Local:      http://localhost:8080/api
#   Production: https://croffy-api.onrender.com/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
